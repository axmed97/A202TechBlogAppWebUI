@model User
@inject Microsoft.AspNetCore.Identity.UserManager<User> _userManager


<dl>
    <dt>Firstname & Lastname</dt>
    <dd>@Model.Firstname @Model.Lastname</dd>
    <dt>Email</dt>
    <dd>@Model.Email</dd>
</dl>

<table class="table table-dark">
    <thead>
        <tr>
            <th>#</th>
            <th>Role</th>
        </tr>
    </thead> 
    <tbody>
        @foreach (var item in await _userManager.GetRolesAsync(Model))
        {
            <tr>
                <td>@item</td>
                <td>
                    <a class="btn btn-danger" asp-area="Admin" asp-controller="User" asp-action="Delete" asp-route-userId ="@Model.Id" asp-route-role="@item" >Delete Role</a>
                </td>
            </tr>
        }
    </tbody>
</table>